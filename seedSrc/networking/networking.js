
module.exports = {
    /**
     * 
      * Creates a relay node instance which multiple users can connect to
      * Users can both run a regular node and a relay node. Their regular would just point to localhost
     */
    getRelayNode : function() {
    },
    /**
     * Creates a client instance which can connect to a relay node IP
     */
    newClient : function() {

    }
}